import {

} from "../../actions.js";

const initialState = {

};

const PostsReducer = (state = initialState, action) => {
  switch (action.type) {
    case :
      return { ...state };

  }

  return state;
};

export default PostsReducer;
